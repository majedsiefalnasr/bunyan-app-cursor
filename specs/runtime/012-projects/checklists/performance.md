# Performance Checklist — Projects

- [x] Eager-load `customer`, `contractor`, `supervisingArchitect`, and counts where list payload needs them
- [x] Timeline endpoint loads `phases` in one query per project
- [x] Pagination preserved on project index
