# Data Model — API Foundation

This stage does **not** introduce new database tables.

## Entities touched

None.

## Configuration keys

| Key / file        | Purpose                                     |
| ----------------- | ------------------------------------------- |
| `config/cors.php` | Allowed origins, paths, credentials for SPA |
| `config/app.php`  | `name` for health payload                   |
